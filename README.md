# App Message

Este proyecto contiene una aplicación Fullstack (React + Node.js)

## 🛠️ Cómo arrancar el proyecto

1. Clona el repositorio.
2. Ejecuta el comando maestro para compilar e iniciar los contenedores:
   ```bash
   docker-compose up --build
